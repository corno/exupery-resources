#!/usr/bin/env node

import * as _ea from 'exupery-core-alg'
import * as _eb from 'exupery-core-bin'
import * as _easync from 'exupery-core-async'

export const $$: _eb.Run_Unguaranteed_Procedure_Main = ($i) => $i.execute(($i) => $i )
